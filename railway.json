{
  "$schema": "https://railway.app/railway.schema.json",
  "services": [
    {
      "name": "Postgres",
      "plugin": "postgresql"
    },
    {
      "name": "Redis",
      "plugin": "redis"
    },
    {
      "name": "backend",
      "build": {
        "builder": "DOCKERFILE",
        "dockerfilePath": "backend/Dockerfile"
      },
      "variables": {
        "DATABASE_URL": "${{Postgres.DATABASE_URL}}",
        "REDIS_URL": "${{Redis.REDIS_URL}}",
        "JWT_SECRET": {
          "description": "JWT signing secret — generate any long random string (e.g. 64 random characters)",
          "required": true
        },
        "ADMIN_EMAIL": {
          "description": "Email address you will use to log in to the dashboard",
          "default": "admin@example.com"
        },
        "ADMIN_PASSWORD": {
          "description": "Password for the admin dashboard account — choose a strong password",
          "required": true
        },
        "NODE_ENV": {
          "description": "Node environment",
          "default": "production"
        },
        "PORT": {
          "description": "Port the backend listens on",
          "default": "3000"
        },
        "PUBLIC_URL": "https://${{RAILWAY_PUBLIC_DOMAIN}}",
        "TWILIO_ACCOUNT_SID": {
          "description": "Twilio Account SID (optional — can also be configured via the in-app Settings wizard after deploy)"
        },
        "TWILIO_AUTH_TOKEN": {
          "description": "Twilio Auth Token (optional — can also be configured via the in-app Settings wizard after deploy)"
        },
        "TWILIO_WHATSAPP_NUMBER": {
          "description": "Twilio WhatsApp sender number in format whatsapp:+1234567890 (optional — configurable in-app)"
        }
      }
    },
    {
      "name": "dashboard",
      "build": {
        "builder": "DOCKERFILE",
        "dockerfilePath": "dashboard/Dockerfile"
      },
      "variables": {
        "VITE_API_URL": "https://${{backend.RAILWAY_PUBLIC_DOMAIN}}/api/v1"
      }
    }
  ]
}
